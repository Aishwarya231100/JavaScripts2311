"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using nodejs, not browser

console.log(3
    +3) // code readability should be high

    let name = ("Aishwarya") //String
    let age = 25 //Number
    let isLoggedIn = false //Boolean
    let state; //Undefined

    /*
    Primitive Data Types
    Numbers = 2 to power 53
    bignit = use in stock market big number
    string = " " also using ' '
    boolean = true / flase
    null = stand alone value in js and null is special type and also empty 
    undefine = default value
    symbole = used in react and find the unique vale 

    */ 

    //check type of 
    console.log(typeof undefined); //Output = Undefined 
    console.log(typeof null); //Output  = Object
    console.log(typeof name); // Output = string


    
    
    
    